<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group25.proj2">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".FractionsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".StoryActivity" />
        <activity android:name=".MovementActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".LastGameActivity" />
        <activity android:name=".TicTacToePromptActivity" />
        <activity android:name=".TicTacToeActivity" />
        <activity android:name=".BluetoothActivity"></activity>
        <activity android:name=".DoneActivity" />
    </application>

</manifest>